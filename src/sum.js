//testing javascript in react
function sum(a, b) {
  return a + b;
}
module.exports = sum;

const add = (x, y) => {
  return x + y;
};
module.exports = add;

// const total = (subTotal, total) => {
//   return "$" + add(subTotal, total);
// };
// module.exports = total;
